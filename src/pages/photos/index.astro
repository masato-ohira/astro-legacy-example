---
import { PhotosPage } from '@/components/views/PhotosPage'
import Layout from '@/layouts/Layout.astro'

const meta = {
  title: 'React App',
  id: 'index',
}
const isDev = process.env.NODE_ENV == 'development'
---

<Layout meta={meta}>
  <PhotosPage client:load />
</Layout>
